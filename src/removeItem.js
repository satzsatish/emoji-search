import React, { PureComponent } from "react";

const removeEmoji = (props) => {
	return <button className="info" onClick={() => this.removeItemHandler(this.props.title)}>Remove</button>
}

export default removeEmoji;